name: Release Linux Binary

on:
  push:
    tags:
      - 'v*' # v1.0.0 などのタグを打った時だけ
jobs:
  release:
    runs-on: ubuntu-latest
    # ここから追加
    permissions:
      contents: write  # Releaseを作ってファイルを置くため
      repository-projects: write # About 編集に必要になる場合がある
    # ここまで追加
    steps:
      - name: Checkout Code
        run: |
          git init
          git branch -M main
          git remote add origin https://github.com/${{ github.repository }}.git
          git config --local http.https://github.com/.extraheader "AUTHORIZATION: basic $(echo -n x-access-token:${{ secrets.GITHUB_TOKEN }} | base64 )"
          git fetch --depth=1 origin ${{ github.ref }}
          git checkout FETCH_HEAD

      - name: Setup Rust (Node Free)
        run: |
          rustup update stable
          rustup default stable

      - name: Build
        run: cargo build --release --locked

      - name: Create Distribution Package
        run: |
          TAG_NAME=${{ github.ref_name }}
          mkdir dist
          cp target/release/spc dist/
          cp -r data/ dist/
          cp -r docs/ dist/
          cp -r output dist/
          cp README.md dist/
          cp CHANGELOG.md dist/
          cp LICENSE dist/
          
          tar -cvzf spc_${TAG_NAME}_linux.tar.gz -C dist .

      - name: Extract latest changelog
        id: extract_changelog
        run: |
          # 1つ目の '##' から 2つ目の '##' の直前までを抜き出すなり
          # ※ 最後の 'EOF' は GitHub Actions の「複数行出力」のお作法なり
          {
            echo 'body<<EOF'
            sed -n '/^## \[[0-9].*\]/,/^## \[[0-9].*\]/p' ./dist/CHANGELOG.md | sed '$d'
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      # - name: Update Repository Metadata
      #  env:
      #    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #  run: |
      #    # Cargo.tomlからversionやdescriptionを抜いて、Aboutに反映させるなり！
      #    DESC=$(grep '^description' Cargo.toml | sed 's/description = "//;s/"//')
      #    gh repo edit --description "$DESC" --add-topic "rust"

      - name: Upload to Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME=${{ github.ref_name }}
          
          # 既存のリリースがあれば削除してから作り直す（リトライ対策）
          gh release delete "$TAG_NAME" --yes || true
          
          gh release create "$TAG_NAME" \
            spc_${TAG_NAME}_linux.tar.gz \
            --title "Survival Painter $TAG_NAME" \
            --notes "${{ steps.extract_changelog.outputs.body }}"
